
import userInterface.*;

import javax.swing.*;

public class Main {
    public static void main(String[] args) {

        programWindow window = new programWindow();
        //targetSelectionUI UI = new targetSelectionUI(); //I think we should separate our features into diff classes from the window, then use them in the window class

    }
}
